import { EditFocusDirective } from './edit-focus.directive';

describe('EditFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new EditFocusDirective();
    expect(directive).toBeTruthy();
  });
});
