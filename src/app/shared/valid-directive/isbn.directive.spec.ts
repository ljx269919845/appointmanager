import { IsbnDirective } from './isbn.directive';

describe('IsbnDirective', () => {
  it('should create an instance', () => {
    const directive = new IsbnDirective();
    expect(directive).toBeTruthy();
  });
});
